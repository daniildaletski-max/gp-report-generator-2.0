/**
 * Globals injected by Vite's `define` config — see `vite.config.ts`. The
 * footer prints these so we can verify which commit Manus actually deployed
 * just by looking at the page.
 */
declare const __BUILD_SHA__: string;
declare const __BUILD_TIME__: string;
